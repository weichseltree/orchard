# generated from /home/manuel/weichseltree/someotherlife/orchard.yaml — edit that file, not this one
name: someotherlife
path: /home/manuel/weichseltree/someotherlife
remote: https://github.com/weichseltree/someotherlife.git
question: Photograph a room with one phone, then stand in it with a headset on. How far can you lean before
  it stops being a room?
status: active
stage: planted
gpu: torch
phenomena: []
artefacts: []
producers:
  tape: ''
  render: ''
  figure: ''
  lane: none
  env: {}
theses:
- id: lean-until-it-tears
  question: A room shot on one phone, stood in with a headset. How far can you lean before the walls come
    apart?
  number: The measured lean radius in metres at the capture's standing point, beyond which parallax breaks,
    taken in the Quest 3 browser at 72 Hz and checked at 0.5 m and 1.0 m offsets.
  picture: One corner of the room seen from where the viewer stands, and the same corner a metre to the
    side, where the far wall tears into floating smears.
  phenomena: []
  stage: thesis
  episode: ''
  blocked_by: the shoot
budget:
  gpu_h: 5.0
  cpu_h: 4.0
  tts_chars: 0
  llm_usd: 0.0
  disk_gb: 15.0
  prio_cap: 10
kill: Prune as a donor if Spike C finds that no phone-captured room holds 72 Hz in the Quest 3 browser
  and keeps its parallax at a 0.5 m lean, or if no room has been shot by 2026-11-30. The game built here
  goes on either way; it is not the orchard's to fund or prune.
potential: 0
notes: 'Donor tree, not an episode tree. Nothing on disk is a splat, still, clip or tape. Spike C (docs/milestones.md)
  has not run, so there is no captured room and no measured 6DoF budget. Every splat number from this
  tree is a prior until Spike C''s ADR lands; the repo keeps unmeasured numbers out of its docs (ADR 0006).
  Spike C is funded (2026-09-12, the orchard grounds first). Its toolchain is committed on branch spike-c/capture-tooling
  at 29d931d, not yet pushed: tools/capture (pycolmap SfM on the CPU lane, gsplat MCMC on the GPU lane,
  a printed marker for metres and the station frame; ADR 0013), the shoot in docs/shoot.md. `capture job
  <id>` pins the photos by hash and prints the lanepush and exp commands. The rasterizer and training
  loop have never run: the first real capture is also their first run. The budget (5 GPU-h, 4 CPU-h, 15
  GB) is still the estimate; SfM on a synthetic room extrapolates to 45-60 CPU-min for ~250 photos. Exists
  but deliberately not listed as artefacts: a playable WebXR chest room of draft proxies (apps/client,
  `pnpm dev`; desktop orbit fallback plus Enter VR; covered by apps/client/tests/play.spec.ts), 20 SVG
  wardmark glyphs (content/art/wardmarks/), and 25 synthesized voice lines (content/audio/vo/00-the-cellar/,
  Opus with provenance.json). The splat asset''s metadata already has a schema, SplatAssetSchema in packages/scene-schema/src/capture.ts
  (lods of srcUrl + splatCount, boundsM, coverage.radiusM, captureId, consentRef). Splat rooms are station-bound
  by design (docs/vision.md constraint 12): the viewer leans inside a measured radius and never walks.
  The grove''s XR entry already follows this repo''s rule. What is left worth lifting is apps/client/src/input/dwell.ts
  + input/raycast.ts (pointing with a 300 ms dwell, mouse and controller rays; only three and two branded
  string types) and xr/support.ts (a newer probe always wins over an older one). None of it uses eval
  or Function.'
